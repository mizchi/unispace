{
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "run-p build:* && run-s build-demo",
    "build-demo": "yarn workspace unispace-example run build",
    "build:editor": "yarn workspace unispace-editor run build",
    "build:filer": "yarn workspace unispace-filer run build",
    "build:layout": "yarn workspace unispace-layout run build",
    "dev": "run-p dev:*",
    "dev:editor": "yarn workspace unispace-editor run dev",
    "dev:filer": "yarn workspace unispace-filer run dev",
    "dev:layout": "yarn workspace unispace-layout run dev",
    "dev:example": "yarn workspace unispace-example run dev"
  },
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "@types/lodash-es": "^4.17.3",
    "@types/node": "^13.11.1",
    "@types/react": "^16.9.34",
    "@types/react-dom": "^16.9.6",
    "jest": "^25.4.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.0.4",
    "ts-jest": "^25.4.0",
    "typescript": "^3.8.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
